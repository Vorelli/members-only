extends layout

block content
  .content
    .title
      h1= title
    form(method='POST' action='')
      form-group
        label(for='title') Title:
        input(type='text' name='title' placeholder='Title')
      form-group
        label(for='messageBody') Message Body:
        textarea(name='messageBody' rows='4' cols="50")